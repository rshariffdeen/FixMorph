#include <assert.h>
#include <stdio.h>

long int sum (int a, int b)
{
    return a + b ;
}

int main(void)
{
    int i, b=j, k = 7;
    scanf("%d", &i);
    printf("fact: %lu\n", add(i,k));

    return 0;
}