#include <stdio.h>
#include <string.h>
#include <ctype.h>  



int main( ) {
    int j = 2;
    if (j > 0){
    j += 3;
    testLocalRefA(j, "test");
    }
    for (int l =0; ;)
        printf("RANDOM");
    printf("CHECK");

    return 0;
}
