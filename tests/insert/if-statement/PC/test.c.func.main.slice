#include <stdio.h>
#include <string.h>
#include <ctype.h>  



int main( ) {
    int k = 2;
    if (k > 0){
    k += 3;
    testLocalRefC(k, "test");
    }
    for (int l =0; ;)
        printf("RANDOM");
    printf("CHECK");
    return 0;
}
