#include <stdio.h>
#include <string.h>
#include <ctype.h>  



int main( ) {
    int j = 2;
    if (j > 0){
        if (j == 2)
            j += 3;
        testLocalRefA(j, "test");
    }

    if (j > 10){
       for (int l =0; ;)
        printf("RANDOM");
    }

    if (j < 0)
       printf("OK");
    else
       printf("CHECK");

    return 0;
}
